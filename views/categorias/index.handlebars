<div class="card">
  <div class="card-body">
    <h2>Lista de Categorias</h2>
    <hr />
    <ul>
      {{#each categorias}}

        <a href="/categorias/{{slug}}"><li>{{nome}}</li></a>

      {{else}}
        <h2>Não existem categorias</h2>

      {{/each}}
    </ul>
  </div>
</div>